@using PagedList
@using PagedList.Mvc
@model PagedList<QqhrCitizen.Models.ResourceLink>
@{
    ViewBag.Title = "资源链接管理";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div>
    <div class="result-title">
        <h1>类型管理 <a href="/Admin/AddLink">增加</a></h1>
    </div>
    <div class="result-content">

        <table class="detail-table">
            <thead>
                <tr>
                    <td>编号</td>
                    <td>名称</td>
                    <td>路径</td>
                    <td>链接类型</td>
                    <td>增加时间</td>
                    <td>操作员</td>
                    <td>操作</td>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count > 0)
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ID</td>
                            <td>@item.Title</td>
                            <td>@item.URL</td>
                            <td>@item.TypeDictionary.TypeValue</td>
                            <td>@item.Time</td>
                            <td>@item.User.Username</td>
                            <td>
                                <a href="javascript:Delete(@item.ID);">删除</a>
                                <a href="/Admin/LinkEdit/@item.ID">编辑</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        @Html.PagedListPager((IPagedList)Model, page => Url.Action("TypeManager", new { page }))
    </div>
</div>


<script>
    function Delete(id) {
        window.location = '/Admin/LinkDelete/' + id + '?sid=@ViewBag.SID';
    }
</script>

